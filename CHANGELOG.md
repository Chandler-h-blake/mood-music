# 变更记录

本文件记录会影响用户、数据、接口、部署或兼容性的变化。格式参考 Keep a Changelog，版本遵循语义化版本。

## Unreleased

### Changed

- 将手动粘贴 QQ 音乐 Cookie 确认为首版正式登录方案；Cookie 由 Windows 凭据存储或 DPAPI 保护，只供本机只读同步使用。
- 将 QQ 音乐接入拆分为 Python 只读数据适配器和 Manifest V3 网页播放连接器；二维码登录留作后续评估。
- 明确禁止 QQ 音乐写操作、音频直链、代理、下载、缓存和解密进入首版范围。
- DeepSeek 批量画像遇到不合规输出时自动重试并递归拆分批次；失败任务可从已有 checkpoint 继续，不重复处理已完成歌曲。
- 临时队列按搜索会话、歌单、候选歌曲的外键依赖顺序落库，避免首次生成时事务插入顺序不确定。
- 修正 BGE-M3 余弦相似度被重复映射导致的分数虚高，并将均衡选歌默认门槛校准到 75%。
- 只有用户原文明确提及悲伤、紧张或能量边界时才保留对应上限，避免模型凭空扩大硬排除范围。
- 播放阶段调整为 Chrome 网页连接器与 QQ 音乐 PC 连接器双路线；两者共享版本化能力协议，PC 路线先经过安全可行性门槛且禁止进程注入。

### Added

- 建立产品、架构、数据、接口、质量和开发规范文档。
- 建立 Web、API、浏览器扩展、共享契约、基础设施和端到端测试目录。
- 确定独立 Web 应用与 QQ 音乐网页版连接器的目标形态。
- 建立首个可运行的 FastAPI 纵向切片：Windows 凭据存储中的 QQ 音乐 Cookie、登录验证和“我喜欢”第一页只读预览。
- 使用 Windows DPAPI 加密文件保存长 Cookie，避免 Credential Manager 单条凭据容量限制导致保存失败。
- 允许开发进程通过 `MOODMUSIC_DATA_DIR` 把 DPAPI 密文写入 Git 忽略的数据目录，避免受控开发环境阻止标准用户目录写入。
- 增加 PostgreSQL 16 + pgvector Docker Compose、本地健康检查和 Alembic 首版音乐库迁移。
- 增加 QQ 音乐“我喜欢”自动全分页、完整性保护、平台 ID 去重与缺失 ID 稳定哈希回退。
- 增加原子增量同步批次、歌曲与喜欢成员关系持久化，以及本地曲库分页查询 API。
- 增加 Next.js 曲库工作台，支持真实歌曲列表、响应式分页、歌曲/歌手/专辑搜索和一键同步反馈。
- 增加本机 Web 来源白名单与曲库搜索参数，前端不接触或持久化 QQ 音乐 Cookie。
- 增加独立的 DeepSeek API Key DPAPI 凭据、去敏设置状态与连接验证接口。
- 增加 DeepSeek 结构化歌曲画像、本地 BGE-M3 1024 维 pgvector Embedding、持久化画像任务与批次 checkpoint。
- 增加自然语言意图结构化、全量喜欢歌曲评分、明确悲伤/紧张/流派硬排除和合法 0 结果。
- 增加搜索会话、候选审计记录、匹配度排序临时队列及对应的 Next.js 操作界面。
- 完成 M4 候选编辑：匹配度、情绪曲线、可复现随机三种排序，候选删除、拖动、单曲找相似、会话追加要求以及队列快照历史回放。
- 增加歌曲自由文本标签的本地标准化层，统一常见语言、人声类型和曲风别名，无需重新调用模型画像即可用于筛选。
- 自然语言意图新增人声模式、演唱语言、纯音乐许可和最高能量等可执行硬约束；队列页面会展示实际执行的约束。
- 增加 ADR 004，记录 Chrome 优先、PC 后续接入的双播放连接器架构与回退规则。
- 增加 Chrome 与 PC 共用的连接器 v1 消息信封、能力枚举、目录搜索、队列、播放动作与状态 Schema，以及 TypeScript 适配器接口。
- 增加连接器协议描述和无状态能力协商 API，并用同一组测试验证两种连接器类型。
- 增加最小权限的 Chrome Manifest V3 扩展骨架，可发现 QQ 音乐标签页、只读探测页面控件并与本机 API 协商能力。
- 增加 QQ 音乐 PC 只读能力探测脚本，可检查进程、主窗口、客户端版本和 UI Automation 语义化控件，不执行点击或注入。
- 根据首次实机结果收紧 Chrome 队列选择器，区分“仅可搜索”与“播放就绪”；PC 探测改用顶层窗口枚举并明确识别托盘后台状态。
